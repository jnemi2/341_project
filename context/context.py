global model  # type: dict
global view  # type: dict


def initiate():
    """ Initiates the global variables of the MVC context
    """
    global model
    global view
    model = dict()
    view = dict()
